<% content_for(:title, "Image Compression - DocPack") %>

<div class="container">
  <h1>Image Compression</h1>
  <p>Compress images to under 2MB while maintaining quality.</p>

  <%= form_with url: conversions_path, method: :post, data: { turbo: true } do |f| %>
    <%= f.hidden_field :conversion_type, value: "compress" %>

    <div class="upload-zone" id="upload-zone" data-controller="upload">
      <p>Drag & drop images here or click to select</p>
      <%= f.file_field :files, multiple: true, accept: "image/jpeg,image/png,image/heic,image/webp",
          data: { upload_target: "input", action: "change->upload#preview" } %>
    </div>

    <div id="preview-area" data-upload-target="preview"></div>

    <%= f.submit "Compress", class: "btn btn-primary" %>
  <% end %>

  <turbo-frame id="conversion_result"></turbo-frame>
</div>
