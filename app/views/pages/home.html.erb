<% content_for(:title, t("home.title")) %>

<div class="container">
  <header class="hero">
    <h1><%= t("home.heading") %></h1>
    <p><%= t("home.description") %></p>
  </header>

  <nav class="feature-nav">
    <%= link_to t("home.compress"), compress_path, class: "feature-card" %>
    <%= link_to t("home.pdf"), pdf_path, class: "feature-card" %>
    <%= link_to t("home.social"), social_path, class: "feature-card" %>
  </nav>
</div>
