# DECISIONS — DocPack / SlimFile

> **지우지 않는다.** 결정이 바뀌면 옛 행에 ~~취소선~~ + (폐기 YYYY-MM-DD · 사유) 를 남기고
> 새 행을 추가한다. 이 파일의 존재 이유 = "왜 그렇게 했나 / 왜 안 하기로 했나" 를 남기는 것.
> 지운 결정은 3개월 뒤 반드시 누군가 다시 제안한다.

## 결정

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-04-07 | `SOLID_QUEUE_IN_PUMA=true` | 별도 워커 컨테이너가 없어 recurring job 이 아예 실행되지 않았다. Puma 안에서 스케줄러/워커를 인라인으로 돌린다 |
| 2026-07-19 | SafeFile 마스킹을 텍스트 재작성 → **좌표 기반 오버레이**로 전환 | 기존 방식은 원본 레이아웃(표·도장·서명)을 전부 잃었다. 원본을 캔버스에 렌더 후 PII 좌표에 검은 바를 덮고 flatten 하면 레이아웃이 보존된다 |
| 2026-07-19 | SafeFile 출력은 **래스터**로 flatten (PDF 도 페이지별 JPEG 임베드) | 텍스트 레이어가 남으면 마스킹 아래 원문이 추출된다. 검증 결과 추출 가능 텍스트 0, PII 유출 0 |
| 2026-07-19 | 워터마크는 **하단 14% · 상단 5% 를 비워둠**. QR 자동 탐지는 하지 않음 | QR/바코드/도장을 가리는 사고를 막되, 자동 탐지는 과하다고 판단. 대신 4개 언어 안내문으로 사용자가 직접 확인하게 한다 |
| 2026-07-19 | docx/txt/붙여넣기 입력도 계속 지원 | 시각적 원본이 없어 좌표 방식과 어긋나지만, `renderTextPages()` 로 A4 캔버스에 얹어 처리. 사용 사례가 있어 드롭하지 않기로 함 |
| 2026-08-25 | `.kamal/secrets` 가 `.env.production.local` 을 **직접 읽는다** | Kamal 2 가 dotenv 자동 로딩을 제거해, 사전에 `source` 하지 않으면 빈 자격증명으로 `docker login` 이 실패했다. 파일을 직접 읽으면 `kamal deploy` 만으로 동작하고 토큰은 여전히 git 밖에 남는다 |
| 2026-08-25 | `.kamal/secrets` 에는 **`$(...)` 만** 쓴다 (`${VAR:-default}` 금지) | Kamal 파서가 bash 기본값 문법을 지원하지 않으면서 실패하지도 않는다. 리터럴 `:-<값>}` 을 그대로 내보내 레지스트리가 `denied: denied` 로 거절하는데, 이게 토큰 만료와 구분이 안 된다 |
| 2026-08-25 | 업로드 목록의 진실의 원천은 **`input.files`** — `upload_controller` 가 `DataTransfer` 로 다시 써넣는다 | 기존 제출 경로(`progress_controller`)가 `input.files` 를 그대로 읽으므로, 별도 상태를 두지 않으면 제출 로직을 건드릴 필요가 없다. 무효 파일은 목록에는 남기고 `input.files` 에서만 빼면 "보이는 것 = 올라가는 것" 이 유지된다 |
| 2026-08-25 | 파일명 생략은 **head span(ellipsis) + tail span(flex-shrink:0)** 2분할 | CSS `text-overflow` 는 끝만 잘라 확장자를 없앤다. 확장자 + 최소 10자를 tail 로 고정하면 어떤 폭에서도 확장자가 보인다 |
| 2026-08-25 | `input.files` 쓰기가 불가능한 브라우저에서는 **읽기 전용 목록으로 격하**(×버튼 숨김·누적 끔) | ×를 눌러 행이 사라져도 실제로는 업로드되는 상태가 되면 목록이 거짓말을 한다. 안내문을 띄우고 클릭 업로드만 살린다 |
| 2026-08-25 | 파일 목록 카드는 **서비스 컬러**를 입는다 — 상단 4px 액센트 바(풀 강도) + 테두리(38%) + 헤더 밴드(연한 톤 55%) | 흰 드롭존 바로 밑의 흰 카드 + 0.5px 헤어라인이라 목록이 추가돼도 안 보였다. 중립 회색으로는 아무리 강화해도 주변 카드와 구별이 안 된다. 컨트롤러가 zone 의 `upload-zone--*` 클래스를 읽어 `--file-accent`(연한 톤) / `--file-accent-strong`(진한 톤) 두 값을 넘긴다 |
| 2026-08-25 | 액센트 바는 `::before` 가 아니라 **카드의 `border-top: 4px`** | `--r-card` 가 20px 이라 가상요소는 라운드에 잘려 양끝이 뾰족한 조각이 된다. border 로 하면 모서리에서 4px→1px 로 자연스럽게 좁아진다. 대신 카드에 `overflow: hidden` 을 걸어 헤더 밴드가 같은 안쪽 곡선으로 잘리게 했다 |
| 2026-08-25 | 헤더 밴드는 연한 톤을 **55% 로 희석**한다 (액센트 바·테두리는 풀 강도 유지) | 100% 로 두니 `--pdf-light` 를 하드코딩한 `.upload-notice--warn` 과 /pdf 에서 같은 색이 돼 경고가 경고로 안 읽혔다. 경고문은 카드 밖 degraded-mode 안내에도 쓰여 영향 범위가 넓으므로, **이번에 새로 만든 밴드 쪽이 양보**한다. 구분 신호는 바·테두리가 담당한다. 행 진입 하이라이트는 순간 신호라 희석하지 않는다 |
| 2026-08-25 | 새 행 진입 애니메이션 520ms, `prefers-reduced-motion` 에는 **별도 키프레임**(색 페이드만) | 이동·페이드를 없애되 색 변화는 모션이 아니므로 남긴다 — "이 행이 새로 왔다"는 신호가 완전히 죽지 않는다. `render()` 가 매번 전체를 다시 그리므로 직전 렌더의 key 집합과 비교해 새 행만 표시한다 |
| 2026-08-25 | `color-mix()` 는 **폴백 선언 뒤에** 둔다 (`border: 1px solid var(--border-color)` → `border-color: color-mix(...)`) | 미지원 브라우저는 뒷줄을 무시하고 중립 회색 테두리로 내려앉는다. 커스텀 속성 안에 `color-mix` 를 넣으면 값이 통째로 무효가 돼 이 안전망이 사라진다 |

## 기각·보류 (되살리려면 이유부터 다시 볼 것)

| 날짜 | 안 | 왜 안 하나 |
|---|---|---|
| (시점 미상) | 블로그 히어로 이미지에 Google Imagen 사용 | 유료 전용. 현재는 Claude API 로 SVG 를 생성한다. 유료 전환 시 `BlogImageService` 에서 Imagen 4.0 으로 교체 가능 |
| 2026-07-19 | SafeFile 워터마크용 QR/바코드 자동 탐지 | 구현 비용 대비 이득이 낮다고 판단. 여백 규칙 + 안내문으로 대체 |
| 2026-08-25 | `.kamal/secrets` 에서 셸 환경변수 override 를 허용 (`${VAR:-...}`) | 위 결정 참조 — Kamal 파서가 이 문법을 지원하지 않아 오히려 값을 손상시킨다. override 가 필요하면 파일을 직접 수정할 것 |
